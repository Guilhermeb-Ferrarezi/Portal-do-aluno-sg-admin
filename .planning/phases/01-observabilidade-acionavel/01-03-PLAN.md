---
phase: 01-observabilidade-acionavel
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - web/src/services/api/activityLogs.ts
  - web/src/services/api/monitoring.ts
  - web/src/pages/ActivityLogs.tsx
  - web/src/pages/AdminObservability.tsx
autonomous: true
requirements:
  - OBSV-01
user_setup: []
must_haves:
  truths:
    - "Admin consegue ver rota, resultado, request_id e tipo de erro usando as telas administrativas existentes."
    - "A tela de observabilidade destaca melhor auth, presence, activity logs e metrics sem criar tela nova."
  artifacts:
    - path: web/src/services/api/activityLogs.ts
      provides: "Tipos do contrato de activity logs com campos observaveis adicionais"
      min_lines: 20
      contains: "requestId"
    - path: web/src/pages/ActivityLogs.tsx
      provides: "Exibicao acionavel dos novos campos de observabilidade"
      min_lines: 250
      contains: "requestId"
    - path: web/src/pages/AdminObservability.tsx
      provides: "Leitura admin refinada para rotas criticas"
      min_lines: 250
      contains: "presence"
  key_links:
    - from: web/src/services/api/activityLogs.ts
      to: /activity-logs
      via: "apiFetch do endpoint admin existente"
      pattern: "/activity-logs"
    - from: web/src/pages/ActivityLogs.tsx
      to: web/src/services/api/activityLogs.ts
      via: "tipos e consumo dos campos novos"
      pattern: "listarActivityLogs"
    - from: web/src/pages/AdminObservability.tsx
      to: web/src/services/api/monitoring.ts
      via: "snapshot de metricas e destaque de rotas criticas"
      pattern: "fetchMonitoringSnapshot"
---

<objective>
Consumir e apresentar a observabilidade enriquecida nas telas administrativas ja existentes, sem criar tela nova.

Purpose: transformar os sinais novos do backend em leitura pratica para admins nas paginas `ActivityLogs` e `AdminObservability`.
Output: tipos atualizados de monitoring/activity logs e UI administrativa com destaque para rota, request_id, resultado, erro e fluxos criticos.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-observabilidade-acionavel/01-CONTEXT.md
@web/src/services/api/activityLogs.ts
@web/src/services/api/monitoring.ts
@web/src/pages/ActivityLogs.tsx
@web/src/pages/AdminObservability.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar os contratos frontend para os novos sinais de observabilidade</name>
  <files>web/src/services/api/activityLogs.ts, web/src/services/api/monitoring.ts</files>
  <read_first>web/src/services/api/activityLogs.ts, web/src/services/api/monitoring.ts, api/src/routes/activityLogs.ts, web/src/pages/AdminObservability.tsx</read_first>
  <action>Expandir o tipo `ActivityLog` em `web/src/services/api/activityLogs.ts` para aceitar campos opcionais `requestId`, `route`, `statusCode`, `outcome`, `errorType` e `source`, mantendo `metadata`, `ipAddress` e `userAgent`. Em `monitoring.ts`, adicionar helpers locais para destacar rotas criticas conhecidas (`/auth`, `/presence/socket-ticket`, `/presence/heartbeat`, `/activity-logs`, `/metrics`) sem mudar o endpoint nem criar busca nova; isso pode ser feito por funcoes puras que classifiquem rotas ou agrupem breakdown items. Nao criar servico novo, nao alterar `API_BASE_URL`, nao introduzir rota nova.</action>
  <verify>rg -n "requestId|statusCode|outcome|errorType|source|presence/heartbeat|activity-logs" web/src/services/api/activityLogs.ts web/src/services/api/monitoring.ts</verify>
  <acceptance_criteria>
    - `web/src/services/api/activityLogs.ts` contains `requestId?: string`
    - `web/src/services/api/activityLogs.ts` contains `statusCode`
    - `web/src/services/api/activityLogs.ts` contains `outcome`
    - `web/src/services/api/monitoring.ts` contains `presence/heartbeat`
    - `web/src/services/api/monitoring.ts` contains `activity-logs`
  </acceptance_criteria>
  <done>O frontend entende os campos novos do backend e consegue destacar rotas criticas sem tela ou endpoint adicional.</done>
</task>

<task type="auto">
  <name>Task 2: Tornar a tela de Activity Logs acionavel para diagnostico</name>
  <files>web/src/pages/ActivityLogs.tsx</files>
  <read_first>web/src/pages/ActivityLogs.tsx, web/src/services/api/activityLogs.ts, web/src/components/Dashboard/DashboardLayout.tsx, .planning/phases/01-observabilidade-acionavel/01-CONTEXT.md</read_first>
  <action>Estender `ActivityLogs.tsx` para exibir os campos novos nos cards e no detalhe expandido, sem mudar a navegacao nem criar pagina nova. Adicionar badges/linhas para `requestId`, `route`, `statusCode`, `outcome`, `errorType` e `source` quando vierem do backend. Incluir opcoes novas de filtro ou mapeamento visual apenas se reaproveitarem o painel existente; por exemplo, reconhecer `presence` como `entityType` e tratar `login_failed`, `token_refresh`, `logout` e eventos `presence.*` como acoes importantes. Manter a linguagem visual atual, preservar os tabs existentes mesmo que a secao `users` fique menos relevante para esta milestone, e evitar qualquer referencia a alunos como publico do projeto. Nao tocar em auth do frontend nem em rotas da aplicacao.</action>
  <verify>rg -n "requestId|statusCode|outcome|errorType|source|presence" web/src/pages/ActivityLogs.tsx</verify>
  <acceptance_criteria>
    - `web/src/pages/ActivityLogs.tsx` contains `requestId`
    - `web/src/pages/ActivityLogs.tsx` contains `statusCode`
    - `web/src/pages/ActivityLogs.tsx` contains `outcome`
    - `web/src/pages/ActivityLogs.tsx` contains `errorType`
    - `web/src/pages/ActivityLogs.tsx` contains `presence`
  </acceptance_criteria>
  <done>Admins conseguem abrir um log e entender rapidamente rota, resultado, erro e request correlacionado sem sair da tela atual.</done>
</task>

<task type="auto">
  <name>Task 3: Refinar AdminObservability para destacar rotas e riscos operacionais da fase</name>
  <files>web/src/pages/AdminObservability.tsx, web/src/services/api/monitoring.ts</files>
  <read_first>web/src/pages/AdminObservability.tsx, web/src/services/api/monitoring.ts, api/src/server.ts, api/src/routes/presence.ts, api/src/routes/auth.ts</read_first>
  <action>Ajustar `AdminObservability.tsx` para destacar explicitamente os fluxos criticos desta fase usando os dados que ja vem de `/metrics`: auth, presence socket ticket, presence heartbeat, activity logs e metrics. Isso pode ser feito adicionando uma secao curta de rotas criticas, etiquetas ou ordenacao auxiliar baseada nos breakdown items existentes, sempre dentro da pagina atual. Mostrar risco e volume sem inventar alertas em tempo real, sem nova tela e sem polling diferente do atual. Preservar o refresh interval, o parser Prometheus atual e o comportamento de erro. Se criar helpers em `monitoring.ts`, mantelos puros e usados pela pagina.</action>
  <verify>rg -n "auth|presence|activity-logs|metrics" web/src/pages/AdminObservability.tsx web/src/services/api/monitoring.ts</verify>
  <acceptance_criteria>
    - `web/src/pages/AdminObservability.tsx` contains `presence`
    - `web/src/pages/AdminObservability.tsx` contains `activity-logs`
    - `web/src/pages/AdminObservability.tsx` contains `metrics`
    - `web/src/services/api/monitoring.ts` contains `/presence/socket-ticket` or `presence/socket-ticket`
  </acceptance_criteria>
  <done>A pagina administrativa passa a direcionar melhor o olhar do admin para as rotas mais importantes de observabilidade da fase 1.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd web && npm run lint`
- [ ] `cd web && npm run build`
- [ ] `rg -n "requestId|statusCode|outcome|errorType|source" web/src/pages/ActivityLogs.tsx web/src/services/api/activityLogs.ts`
- [ ] `rg -n "presence|activity-logs|metrics|auth" web/src/pages/AdminObservability.tsx web/src/services/api/monitoring.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Admin consegue diagnosticar melhor auth/presence e falhas HTTP usando apenas as telas administrativas existentes
  </success_criteria>

<output>
After completion, create `.planning/phases/01-observabilidade-acionavel/01-03-SUMMARY.md`
</output>
