---
phase: 01-observabilidade-acionavel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/observability/requestObservability.ts
  - api/src/server.ts
autonomous: true
requirements:
  - OBSV-01
  - OBSV-02
user_setup: []
must_haves:
  truths:
    - "HTTP logs completados e falhos carregam request_id, rota normalizada, status e classificacao de resultado sem expor segredos."
    - "Erros 4xx e 5xx das rotas criticas aparecem com contexto operacional consistente no logger estruturado do backend."
  artifacts:
    - path: api/src/observability/requestObservability.ts
      provides: "Middleware e helpers de observabilidade HTTP com contexto enriquecido"
      min_lines: 80
      contains: "http_request_completed"
    - path: api/src/server.ts
      provides: "Wiring do middleware e error handler global sem quebrar auth/presence/proxy"
      min_lines: 250
      contains: "createRequestObservabilityMiddleware"
  key_links:
    - from: api/src/server.ts
      to: api/src/observability/requestObservability.ts
      via: "middleware global e logRequestError no error handler"
      pattern: "createRequestObservabilityMiddleware|logRequestError"
---

<objective>
Fortalecer a trilha tecnica de observabilidade HTTP no backend antes de mexer nas rotas especificas.

Purpose: garantir uma base unica e previsivel para logs estruturados de requests criticos, sem depender de console solto nem quebrar compatibilidade de auth, presence e proxy.
Output: middleware de request observability enriquecido e error handler global com campos acionaveis e normalizados.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-observabilidade-acionavel/01-CONTEXT.md
@api/src/observability/requestObservability.ts
@api/src/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Padronizar o payload de observabilidade HTTP</name>
  <files>api/src/observability/requestObservability.ts</files>
  <read_first>api/src/observability/requestObservability.ts, api/src/server.ts, .planning/phases/01-observabilidade-acionavel/01-CONTEXT.md</read_first>
  <action>Adicionar helpers locais em `requestObservability.ts` para produzir um payload seguro e reutilizavel de request, sem criar dependencia nova. O payload de `http_request_completed` e `http_request_failed` deve incluir explicitamente `request_id`, `method`, `path`, `route`, `status_code`, `status_class`, `outcome`, `duration_ms`, `actor_id`, `actor_role`, `ip`, `user_agent` e `error_type`. Definir `status_class` como `2xx`, `4xx` ou `5xx` com base no status final; definir `outcome` como `success` para 2xx/3xx, `client_error` para 4xx e `server_error` para 5xx. Preservar `SKIPPED_PATHS` para `/health`, `/api/health`, `/metrics` e `/api/metrics`. Normalizar `route` usando `options.metrics.resolveRouteLabel(req)` tambem em `logRequestError`, para evitar divergencia entre logs de sucesso e falha. Usar `AuthRequest` para preencher `actor_id` e `actor_role`; quando nao houver usuario autenticado, retornar `null` nesses campos. Nao registrar headers sensiveis, bodies completos, tokens nem segredos.</action>
  <verify>rg -n "status_class|outcome|actor_id|actor_role" api/src/observability/requestObservability.ts</verify>
  <acceptance_criteria>
    - `api/src/observability/requestObservability.ts` contains `status_class`
    - `api/src/observability/requestObservability.ts` contains `outcome`
    - `api/src/observability/requestObservability.ts` contains `actor_id`
    - `api/src/observability/requestObservability.ts` contains `actor_role`
    - `api/src/observability/requestObservability.ts` contains `options.metrics.resolveRouteLabel(req)` inside both completion and failure logging paths
  </acceptance_criteria>
  <done>O middleware e o helper de falha emitem o mesmo conjunto base de campos acionaveis, com rota normalizada e classificacao de resultado.</done>
</task>

<task type="auto">
  <name>Task 2: Ajustar o wiring global da API para a nova base de logs</name>
  <files>api/src/server.ts</files>
  <read_first>api/src/server.ts, api/src/observability/requestObservability.ts, .planning/PROJECT.md</read_first>
  <action>Manter o middleware `createRequestObservabilityMiddleware` global em `server.ts`, mas ajustar o error handler e o 404 para trabalhar com a base enriquecida. O 404 deve continuar retornando `{ message: "Not Found", path: req.path }`, sem criar rota nova. O error handler global deve continuar chamando `logRequestError(logger, req, res, err)` quando `observabilityEnabled` estiver ativo e deve preservar o fluxo atual de respostas 413, 4xx e 5xx. Se precisar tocar no 404 ou em respostas de erro, manter exatamente os endpoints atuais com e sem prefixo `/api`, sem alterar `shouldSkipApiLimiter`, sem remover `trust proxy` e sem introduzir logs de segredos/env. O objetivo aqui e garantir que o logger estruturado seja a fonte de verdade tecnica para os fluxos acompanhados.</action>
  <verify>rg -n "logRequestError|Not Found|createRequestObservabilityMiddleware" api/src/server.ts</verify>
  <acceptance_criteria>
    - `api/src/server.ts` contains `createRequestObservabilityMiddleware`
    - `api/src/server.ts` contains `logRequestError(logger, req, res, err)`
    - `api/src/server.ts` still contains `message: "Not Found"`
    - `api/src/server.ts` still contains both `/presence/socket-ticket` and `/api/presence/socket-ticket`
  </acceptance_criteria>
  <done>O servidor continua compatível com auth/presence/proxy e passa a depender de uma trilha estruturada única para requests falhos e completados.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd api && npm run build`
- [ ] `rg -n "status_class|outcome|actor_id|actor_role" api/src/observability/requestObservability.ts`
- [ ] `rg -n "logRequestError\\(logger, req, res, err\\)" api/src/server.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Logs HTTP do backend carregam request_id, rota normalizada e classificacao de resultado sem vazar segredos
  </success_criteria>

<output>
After completion, create `.planning/phases/01-observabilidade-acionavel/01-01-SUMMARY.md`
</output>
