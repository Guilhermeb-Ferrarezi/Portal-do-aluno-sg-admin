---
phase: 01-observabilidade-acionavel
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - api/src/utils/activityLog.ts
  - api/src/routes/auth.ts
  - api/src/routes/presence.ts
  - api/src/routes/activityLogs.ts
autonomous: true
requirements:
  - OBSV-01
  - OBSV-02
user_setup: []
must_haves:
  truths:
    - "Eventos de auth e presence registram contexto suficiente para diagnostico sem exigir reproducao cega."
    - "O endpoint de activity logs expõe request_id, rota, resultado e erro relevante sem alterar schema nem exigir tela nova."
  artifacts:
    - path: api/src/utils/activityLog.ts
      provides: "Serializacao segura e consistente dos activity logs"
      min_lines: 40
      contains: "metadata="
    - path: api/src/routes/auth.ts
      provides: "Eventos de autenticacao com contexto observavel"
      min_lines: 250
      contains: "login_failed"
    - path: api/src/routes/presence.ts
      provides: "Eventos de presence com trilha operacional"
      min_lines: 80
      contains: "presence/heartbeat"
    - path: api/src/routes/activityLogs.ts
      provides: "Resposta admin com campos acionaveis de observabilidade"
      min_lines: 120
      contains: "requestId"
  key_links:
    - from: api/src/routes/auth.ts
      to: api/src/utils/activityLog.ts
      via: "logActivity nas rotas de autenticacao"
      pattern: "logActivity\\("
    - from: api/src/routes/presence.ts
      to: api/src/utils/activityLog.ts
      via: "logActivity nos fluxos de socket-ticket e heartbeat"
      pattern: "logActivity\\("
    - from: api/src/routes/activityLogs.ts
      to: api/src/utils/activityLog.ts
      via: "parse da serializacao segura do campo Message"
      pattern: "requestId|statusCode|outcome|errorType"
---

<objective>
Enriquecer os eventos funcionais de auth e presence e expor esse contexto para admins pelo endpoint existente de activity logs.

Purpose: transformar eventos criticos em sinais acionaveis para diagnostico, sem mudar banco, sem criar rota nova e sem quebrar compatibilidade com auth/presence/proxy.
Output: serializacao segura de activity logs, instrumentacao de auth/presence e resposta de `/activity-logs` com campos observaveis novos.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-observabilidade-acionavel/01-CONTEXT.md
@api/src/utils/activityLog.ts
@api/src/routes/auth.ts
@api/src/routes/presence.ts
@api/src/routes/activityLogs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Padronizar serializacao segura dos activity logs</name>
  <files>api/src/utils/activityLog.ts, api/src/routes/activityLogs.ts</files>
  <read_first>api/src/utils/activityLog.ts, api/src/routes/activityLogs.ts, api/src/observability/requestObservability.ts, .planning/phases/01-observabilidade-acionavel/01-CONTEXT.md</read_first>
  <action>Expandir `logActivity` para aceitar e serializar, dentro do `Message`, chaves observaveis seguras alem das ja existentes: `requestId`, `route`, `statusCode`, `outcome`, `errorType`, `source` e `contextArea`. Manter o formato `key=value | ...`, preservar truncamento em 4000 caracteres e continuar gravando apenas na tabela `logs` existente. Em `activityLogs.ts`, ampliar `parseMessageMetadata` para reconhecer essas chaves e retornar no JSON do endpoint, sem remover `metadata`, `ipAddress` e `userAgent`. A resposta de cada item pode ganhar campos top-level opcionais `requestId`, `route`, `statusCode`, `outcome`, `errorType` e `source`, mantendo compatibilidade com consumidores atuais. Nao mudar schema, nao mudar SQL da tabela, nao expor token, senha, refresh token, shared secret ou payload bruto.</action>
  <verify>rg -n "requestId|statusCode|outcome|errorType|source" api/src/utils/activityLog.ts api/src/routes/activityLogs.ts</verify>
  <acceptance_criteria>
    - `api/src/utils/activityLog.ts` contains `requestId`
    - `api/src/utils/activityLog.ts` contains `statusCode`
    - `api/src/routes/activityLogs.ts` contains `requestId`
    - `api/src/routes/activityLogs.ts` contains `outcome`
    - `api/src/routes/activityLogs.ts` contains `errorType`
  </acceptance_criteria>
  <done>O endpoint de activity logs consegue devolver contexto observavel novo sem depender de schema novo nem quebrar o formato anterior.</done>
</task>

<task type="auto">
  <name>Task 2: Instrumentar eventos criticos de auth sem vazar dados sensiveis</name>
  <files>api/src/routes/auth.ts</files>
  <read_first>api/src/routes/auth.ts, api/src/utils/activityLog.ts, api/src/observability/requestObservability.ts</read_first>
  <action>Ampliar os `logActivity` de `auth.ts` para incluir metadados observaveis padronizados. Nos caminhos de falha de `POST /login`, registrar `source: "auth.login"`, `outcome: "denied"`, `statusCode` correspondente e `errorType` sem incluir senha. Em `POST /refresh`, adicionar trilha para `refresh token invalido`, `revogado` e `expirado`, com `source: "auth.refresh"` e sem salvar o token em texto claro; o `refreshTokenId` ja existente pode permanecer. Em `POST /logout`, `GET /sso`, `POST /student-view/start` e `POST /student-view/exchange`, incluir `source`, `outcome` e `statusCode` nas trilhas existentes e nos erros relevantes, sempre sem expor shared secret, codigo SSO ou payload bruto. Preservar respostas atuais, inclusive mensagens e perfis aceitos. Se algum caminho de erro ainda usa apenas `console.error`, manter o `console.error` se necessario para debug local, mas complementar com `logActivity` acionavel quando houver `actorId` ou contexto util.</action>
  <verify>rg -n "source: \\\"auth\\.|outcome:|statusCode:|errorType:" api/src/routes/auth.ts</verify>
  <acceptance_criteria>
    - `api/src/routes/auth.ts` contains `source: "auth.login"`
    - `api/src/routes/auth.ts` contains `source: "auth.refresh"`
    - `api/src/routes/auth.ts` contains `source: "auth.logout"` or `source: "auth.sso"`
    - `api/src/routes/auth.ts` contains `outcome:`
    - `api/src/routes/auth.ts` does not contain `refreshToken` inside `metadata:` payloads passed to `logActivity`
  </acceptance_criteria>
  <done>Falhas e sucessos relevantes de auth passam a carregar contexto operacional util, sem vazar credenciais ou segredos.</done>
</task>

<task type="auto">
  <name>Task 3: Instrumentar presence e expor sinais novos para admins</name>
  <files>api/src/routes/presence.ts, api/src/routes/activityLogs.ts</files>
  <read_first>api/src/routes/presence.ts, api/src/routes/activityLogs.ts, api/src/utils/activityLog.ts, api/src/server.ts</read_first>
  <action>Adicionar `logActivity` em `presence.ts` nos pontos de maior valor diagnostico: `POST /presence/socket-ticket` com falha 401, `POST /presence/heartbeat` com falha 401 e erro 500, e opcionalmente sucesso de heartbeat apenas quando houver contexto suficiente e sem virar ruido excessivo. Usar `entityType: "presence"` e `source` concretos como `presence.socket-ticket` e `presence.heartbeat`; incluir `route`, `statusCode`, `outcome`, `errorType` e um `contextArea: "presence"` quando aplicavel. Em `activityLogs.ts`, garantir que esses eventos aparecam sem cair na secao errada e que filtros por `entityType` e busca continuem funcionais. Preservar compatibilidade das rotas `/presence/*` e `/api/presence/*`, do proxy secret e do heartbeat atual; nao alterar assinatura de resposta nem handshake do WebSocket.</action>
  <verify>rg -n "presence\\.socket-ticket|presence\\.heartbeat|entityType: \\\"presence\\\"" api/src/routes/presence.ts api/src/routes/activityLogs.ts</verify>
  <acceptance_criteria>
    - `api/src/routes/presence.ts` contains `presence.socket-ticket`
    - `api/src/routes/presence.ts` contains `presence.heartbeat`
    - `api/src/routes/presence.ts` contains `entityType: "presence"`
    - `api/src/routes/activityLogs.ts` still contains `/activity-logs` route and `requireRole(["admin"])`
  </acceptance_criteria>
  <done>Presence e auth passam a gerar trilhas funcionais acionaveis que os admins conseguem consumir pelo endpoint existente.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd api && npm run build`
- [ ] `rg -n "requestId|statusCode|outcome|errorType|source" api/src/utils/activityLog.ts api/src/routes/activityLogs.ts`
- [ ] `rg -n "source: \\\"auth\\.|presence\\.socket-ticket|presence\\.heartbeat" api/src/routes/auth.ts api/src/routes/presence.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Auth, presence e activity logs expõem contexto acionavel novo sem schema change e sem quebra de rotas/proxy
  </success_criteria>

<output>
After completion, create `.planning/phases/01-observabilidade-acionavel/01-02-SUMMARY.md`
</output>
